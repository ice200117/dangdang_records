<%= form_with(model: course, local: true, class:"ui form") do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :活动内容 %>
    <%= form.text_field :des, id: :course_des %>
  </div>

  <div class="field">
    <%= form.label :分数 %>
    <%= form.number_field :score, id: :course_score %>
  </div>

  <div class="actions">
    <%= form.submit '提交', class: "ui primary submit button" %>
  </div>
<% end %>
